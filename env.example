# WaveEnv Docker 环境变量配置示例
# 复制此文件为 .env 并根据需要修改配置

# ========== 服务启用控制 ==========
# 是否启用前端服务（设置为空字符串则只启动后端）
# 默认值: frontend（启用）
# 禁用前端: FRONTEND_PROFILE=
FRONTEND_PROFILE=frontend

# ========== 端口配置 ==========
# 后端服务端口
BACKEND_PORT=8000

# 前端服务端口
FRONTEND_PORT=8888

# ========== 后端配置 ==========
# 日志级别 (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# ========== 前端配置 ==========
# 后端服务地址
# Docker 环境下使用服务名: http://backend:8000
# 外部访问使用: http://localhost:8000
BACKEND_URL=http://backend:8000

# ========== 开发模式配置 ==========
# 是否启用开发模式（挂载代码目录，支持热重载）
# DEV_MODE=false

# ========== 资源限制（可选） ==========
# 后端 CPU 限制（如: 2 或 2.0）
# BACKEND_CPU_LIMIT=2
# 后端内存限制（如: 2G）
# BACKEND_MEMORY_LIMIT=2G
# 前端 CPU 限制
# FRONTEND_CPU_LIMIT=1
# 前端内存限制
# FRONTEND_MEMORY_LIMIT=1G

