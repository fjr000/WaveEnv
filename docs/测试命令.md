# 后端测试命令

## 🚀 快速测试（推荐）

### 1. 代码验证
```bash
cd backend
python validate_code.py
```

### 2. 快速功能测试
```bash
cd backend
python quick_test.py
```

### 3. 启动服务测试 API（最简单）

**启动服务：**
```bash
cd backend
uvicorn app.main:app --reload
```

**访问 API 文档：**
```
http://127.0.0.1:8000/docs
```

在 Swagger UI 中直接测试各个 API 端点。

---

## 🧪 使用 pytest 测试

### 修复 httpx 版本问题

如果遇到 TestClient 错误，先降级 httpx：

```bash
pip install "httpx<0.25.0"
```

### 运行测试

```bash
cd backend

# 基础功能测试
pytest ../tests/backend/test_basic.py -v

# API 测试（需要先修复 httpx 版本）
pytest ../tests/backend/test_api.py -v
```

---

## 📝 手动测试脚本

如果 pytest 有问题，使用手动测试：

```bash
# 终端1：启动服务
cd backend
uvicorn app.main:app --reload

# 终端2：运行手动测试
python tests/backend/manual_test.py
```

---

## ✅ 推荐测试流程

1. **先运行快速测试**：`python backend/quick_test.py`
2. **启动服务**：`uvicorn app.main:app --reload`
3. **使用 API 文档**：访问 http://127.0.0.1:8000/docs

这是最简单可靠的测试方式！


